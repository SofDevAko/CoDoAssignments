<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Akin's Mongoose Dashboard</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" type="text/css" media="screen" href="main.css" />
    <script src="main.js"></script>
</head>
<body>
    <h2>Here are the awesome Owls!</h2>
    <br>
    <div id="box">
        <% if( allowls.length > 0 ) { %>
            <% for (var owl of allowls) { %>
                <h3>"<%= owl.name  %>"</h3><br>
                <a href="/owls/show/<%= owl._id %>">View Owl</a>
                <a href="/owls/edit/<%= owl._id %>">Edit Owl</a>
                <a href="/owls/destroy/<%= owl._id %>">Delete Owl</a>
                <hr>
            <% } %>
        <% } %>
    </div>
    <a href="/owls/new">Create a new Owl!</a>
    <% if(typeof(errors) != 'undefined' ) { %>
        <% for (var x in errors) { %>
         <h3><%= errors[x] %></h3>
        <% } %>
    <% } %>
</body>
</html>