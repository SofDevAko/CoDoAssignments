<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Akin's Wedding Planner</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    
</head>
<body>
    <h1>@ViewBag.wedding.WedOne and @ViewBag.wedding.WedTwo 's Wedding</h1>
    <a href="/logout">Not @ViewBag.user.FirstName? Click to Logout</a><br>
    <a href="/dashboard">Dashboard</a>

    <h2>Date: @ViewBag.wedding.Date</h2>
    <h2>Address: @ViewBag.wedding.Address</h2>
    <h3>Attending Guests:</h3>
    <ul>
        @foreach(var guest in ViewBag.wedding.GuestList)
        {
            <li>@guest.User.FirstName @guest.User.LastName</li>
        }
    </ul>
  
</body>
</html>